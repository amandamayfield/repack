services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: repack
      POSTGRES_USER: repack
      POSTGRES_PASSWORD: repack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
